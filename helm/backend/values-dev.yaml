config:
  REDIS_URL: redis://host.k3d.internal:6379
  REDIS_HOST: host.k3d.internal
  KAFKA_BOOTSTRAP_SERVERS: host.k3d.internal:9092

secrets:
  create: true
  existingSecretName: ""
  data:
    DATABASE_URL: postgresql://postgres:postgres@host.k3d.internal:5432/postgres
    SECRET_KEY: local-development-only
    GUARD_API_KEY: local-development-only
    AMADEUS_API_KEY: local-development-only
    AMADEUS_API_SECRET: local-development-only
    MAIL_USERNAME: local@example.com
    MAIL_PASSWORD: local-development-only
    MAIL_FROM: local@example.com
